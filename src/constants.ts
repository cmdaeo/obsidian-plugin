// constants.ts — no hardcoded credentials. Each user registers their own
// OAuth App and enters the Client ID + Secret in plugin Settings.
// This file is kept for the redirect URI only.
export const REDIRECT_URI = "obsidian://git-sync-callback";
